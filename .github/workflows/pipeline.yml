name: Pipeline
on: [push, pull_request]

jobs:
  tests:
     runs-on: ubuntu-latest

     steps:
      - name: Roda um comando!
        run: echo $(pwd)

      - name: Roda outro comando!
        run: echo $(ls)